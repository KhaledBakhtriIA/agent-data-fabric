# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/

# Local run history — schema is versioned instead
data/*.db

# Result files dropped in for ingestion (keep the folder, ignore its contents)
data/imports/*
!data/imports/.gitkeep

# Local agent/tooling instructions — kept on disk, not published to the repo
CLAUDE.md
