"""QA Reliability Intelligence — local-first, framework-agnostic test-run history.

Public entry point is the CLI (``reliability.cli:main``). The package is laid
out as: adapters (parse) → store (persist) → analysers (statistics) → cli.
"""

__version__ = "0.1.0"
